export function tips(score:number,wpm:number,acc:number,cons:number,bs:number,focus:number){const a:string[]=[];if(acc<.9)a.push("trade a little speed for cleaner accuracy");if(wpm<35)a.push("use short 2–4 word bursts to build rhythm");if(cons<.72)a.push("aim for a steadier key-to-key cadence");if(bs>.08)a.push("make lighter, more deliberate corrections");if(focus)a.push("keep your eyes on the screen to avoid focus pauses");return a.slice(0,4).concat(a.length?[]:[score>=85?"keep this control steady while adding speed":"repeat the drill and target one improvement at a time"])}