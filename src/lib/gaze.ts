import type{GazeState}from"../types"; export class GazeMonitor{stream:MediaStream|null=null;detector:any=null;raf=0;history:GazeState[]=[];stopped=true;async start(video:HTMLVideoElement,onState:(s:GazeState)=>void){this.stopped=false;this.stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:"user",width:{ideal:640},height:{ideal:480}},audio:false});video.srcObject=this.stream;await video.play();const{FaceLandmarker,FilesetResolver}=await import("@mediapipe/tasks-vision");const fs=await FilesetResolver.forVisionTasks("https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@1.0.1/wasm");this.detector=await FaceLandmarker.createFromOptions(fs,{baseOptions:{modelAssetPath:"https://storage.googleapis.com/mediapipe-models/face_landmarker/face_landmarker/float16/1/face_landmarker.task",delegate:"GPU"},runningMode:"VIDEO",numFaces:1});this.loop(video,onState)} stop(video?:HTMLVideoElement){this.stopped=true;cancelAnimationFrame(this.raf);this.detector?.close?.();this.detector=null;this.stream?.getTracks().forEach(t=>t.stop());this.stream=null;if(video){video.pause();video.srcObject=null}} private loop(video:HTMLVideoElement,onState:(s:GazeState)=>void){if(this.stopped)return;try{const r=this.detector.detectForVideo(video,performance.now());const p=r.faceLandmarks?.[0];const s=p?classify(p):"unknown";this.history.push(s);if(this.history.length>7)this.history.shift();const k=this.history.filter(x=>x==="keyboard").length,sc=this.history.filter(x=>x==="screen").length;onState(k>=4?"keyboard":sc>=4?"screen":"unknown")}catch{onState("unknown")}this.raf=requestAnimationFrame(()=>this.loop(video,onState))}} function classify(p:any[]):GazeState{const n=p[1],l=p[33],r=p[263],f=p[10],c=p[152];if(!n||!l||!r||!f||!c)return"unknown";const ey=(l.y+r.y)/2,dx=Math.max(.02,Math.abs(r.x-l.x)),fh=Math.max(.08,c.y-f.y),yaw=Math.abs((n.x-(l.x+r.x)/2)/dx),pitch=(n.y-ey)/fh;if(yaw>.75)return"unknown";return pitch>.43?"keyboard":pitch<.38?"screen":"unknown"}