import type{Progress}from"../types"; const KEY="typing-pro-progress-v1"; export const fresh=():Progress=>({version:1,activeSeconds:0,totalPracticeWords:0,bestScore:0,skillMap:{},history:[]}); export function load():Progress{try{return{...fresh(),...(JSON.parse(localStorage.getItem(KEY)||"null")||{})}}catch{return fresh()}} export function save(p:Progress){localStorage.setItem(KEY,JSON.stringify(p))}